setup:
  addons:
    - plan: heroku-redis
      as: REDIS
    - plan: heroku-postgresql
      as: DATABASE

build:
  docker:
    web: Dockerfile

release:
  image: web

run:
  web:
    # No command needed if the ENTRYPOINT script handles everything
